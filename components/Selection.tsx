

const Selection = () => {
  return (
    <div>
        <select className="w-full h-[40px] px-4 pt-2 text-[20px] bg-main-background border-2 border-accent-gray rounded-lg hover:border-add-minus" name="selected" id="">Select</select>
    </div>
  )
}

export default Selection